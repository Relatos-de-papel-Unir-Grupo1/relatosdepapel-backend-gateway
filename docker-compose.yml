version: "3.9"

services:
  gateway:
    build: 
      context: .
      dockerfile: Dockerfile.gateway
    ports:
      [8762:8762]
    env_file:
      - .env    
    networks:
      - relatosdepapel-net  

networks:
  relatosdepapel-net:
    external: true    